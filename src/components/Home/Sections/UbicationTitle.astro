<h3
  class="ubication-title mb-12 text-center text-[40px] font-bold leading-5xl text-secondary-500 lg:mb-6"
>
  ¿Tienes alguna pregunta?
</h3>
<p
  class="ubication-subtitle text-pretty text-center text-xl font-light leading-snug text-white-200"
>
  Déjanos tus datos y nos pondremos en contacto contigo a la brevedad para
  resolver todas tus dudas.
</p>

<style>
  .ubication-title,
  .ubication-subtitle {
    opacity: 0;
    transform: translateY(18px);
  }

  :global(.ubication-content.is-visible) .ubication-title {
    animation: ubication-copy-enter 620ms cubic-bezier(0.22, 1, 0.36, 1) 120ms
      both;
  }

  :global(.ubication-content.is-visible) .ubication-subtitle {
    animation: ubication-copy-enter 620ms cubic-bezier(0.22, 1, 0.36, 1) 220ms
      both;
  }

  @keyframes ubication-copy-enter {
    from {
      opacity: 0;
      transform: translateY(18px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ubication-title,
    .ubication-subtitle {
      animation: none;
      opacity: 1;
      transform: none;
    }
  }
</style>
